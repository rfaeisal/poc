networks:
  poc-pecek-net:
    driver: bridge

volumes:
  postgres-data:
  redis-data:
  mosquitto-data:
  prometheus-data:
  grafana-data:

services:

  # ─────────────────────────────────────────
  # PostgreSQL — Database lokal (optional jika pakai Neon)
  # Jalankan dengan: docker compose --profile db up -d
  # ─────────────────────────────────────────
  postgres:
    image: postgres:16-alpine
    container_name: poc-postgres
    restart: unless-stopped
    networks: [poc-pecek-net]
    profiles: [db]
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-pocptx}
      POSTGRES_USER: ${POSTGRES_USER:-pocptx}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    volumes:
      - postgres-data:/var/lib/postgresql/data
      - ./postgres/init.sql:/docker-entrypoint-initdb.d/init.sql:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-pocptx}"]
      interval: 10s
      timeout: 5s
      retries: 5
    ports:
      - "127.0.0.1:5437:5432"
    deploy:
      resources:
        limits:
          memory: 256M

  # ─────────────────────────────────────────
  # Redis — Cache + Session + Pub/Sub internal
  # ─────────────────────────────────────────
  redis:
    image: redis:7-alpine
    container_name: poc-redis
    restart: unless-stopped
    networks: [poc-pecek-net]
    command: redis-server --requirepass ${REDIS_PASSWORD} --maxmemory 64mb --maxmemory-policy allkeys-lru
    volumes:
      - redis-data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "-a", "${REDIS_PASSWORD}", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    ports:
      - "127.0.0.1:6380:6379"
    deploy:
      resources:
        limits:
          memory: 96M

  # ─────────────────────────────────────────
  # LiveKit — WebRTC SFU (core PTT engine)
  # ─────────────────────────────────────────
  livekit:
    image: livekit/livekit-server:v1.8.2
    container_name: poc-livekit
    restart: unless-stopped
    networks: [poc-pecek-net]
    command: >
      --config /etc/livekit.yaml
      --keys "${LIVEKIT_API_KEY}: ${LIVEKIT_API_SECRET}"
      --redis-host redis:6379
      --redis-password ${REDIS_PASSWORD}
      --node-ip ${PUBLIC_IP:-0.0.0.0}
      --dev
    volumes:
      - ./livekit/livekit.yaml:/etc/livekit.yaml:ro
    ports:
      - "7880:7880"
      - "7881:7881"
      - "50000-50100:50000-50100/udp"
    depends_on:
      redis:
        condition: service_healthy
    deploy:
      resources:
        limits:
          memory: 256M

  # ─────────────────────────────────────────
  # Mosquitto — MQTT broker untuk presence & notifikasi
  # ─────────────────────────────────────────
  mosquitto:
    image: eclipse-mosquitto:2
    container_name: poc-mosquitto
    restart: unless-stopped
    networks: [poc-pecek-net]
    volumes:
      - ./mosquitto/mosquitto.conf:/mosquitto/config/mosquitto.conf:ro
      - ./mosquitto/passwd:/mosquitto/config/passwd:ro
      - ./mosquitto/acl:/mosquitto/config/acl:ro
      - mosquitto-data:/mosquitto/data
    ports:
      - "1884:1883"
      - "8884:8883"
      - "9002:9001"
    deploy:
      resources:
        limits:
          memory: 32M

  # ─────────────────────────────────────────
  # Backend API — Fastify Node.js
  # DATABASE_URL dari .env (Neon atau postgres lokal)
  # ─────────────────────────────────────────
  backend:
    build:
      context: ../backend
      dockerfile: Dockerfile
    container_name: poc-backend
    restart: unless-stopped
    networks: [poc-pecek-net]
    environment:
      NODE_ENV: production
      PORT: 3000
      HOST: 0.0.0.0
      DATABASE_URL: ${DATABASE_URL}
      REDIS_URL: redis://:${REDIS_PASSWORD}@redis:6379
      JWT_SECRET: ${JWT_SECRET}
      JWT_ACCESS_EXPIRES: 15m
      JWT_REFRESH_SECRET: ${JWT_REFRESH_SECRET}
      JWT_REFRESH_EXPIRES: 30d
      LIVEKIT_URL: ws://livekit:7880
      LIVEKIT_API_KEY: ${LIVEKIT_API_KEY}
      LIVEKIT_API_SECRET: ${LIVEKIT_API_SECRET}
      MQTT_URL: mqtt://mosquitto:1883
      MQTT_USERNAME: ${MQTT_BACKEND_USER}
      MQTT_PASSWORD: ${MQTT_BACKEND_PASSWORD}
      CORS_ORIGIN: https://${DOMAIN}
    ports:
      - "127.0.0.1:3000:3000"
    depends_on:
      redis:
        condition: service_healthy
      mosquitto:
        condition: service_started
    deploy:
      resources:
        limits:
          memory: 192M

  # ─────────────────────────────────────────
  # Nginx — Reverse proxy + SSL
  # ─────────────────────────────────────────
  nginx:
    image: nginx:alpine
    container_name: poc-nginx
    restart: unless-stopped
    networks: [poc-pecek-net]
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./nginx/conf.d:/etc/nginx/conf.d:ro
      - /etc/letsencrypt:/etc/letsencrypt:ro
      - ../admin/dist:/var/www/admin:ro
      - ./nginx/certbot:/var/www/certbot:ro
    ports:
      - "80:80"
      - "443:443"
    depends_on:
      - backend
      - livekit
      - mosquitto
    deploy:
      resources:
        limits:
          memory: 32M

  # ─────────────────────────────────────────
  # Prometheus — Metrics collection (optional)
  # Jalankan dengan: docker compose --profile monitoring up -d
  # ─────────────────────────────────────────
  prometheus:
    image: prom/prometheus:latest
    container_name: poc-prometheus
    restart: unless-stopped
    networks: [poc-pecek-net]
    profiles: [monitoring]
    volumes:
      - ./monitoring/prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - prometheus-data:/prometheus
    ports:
      - "127.0.0.1:9090:9090"

  # ─────────────────────────────────────────
  # Grafana — Dashboard monitoring (optional)
  # Jalankan dengan: docker compose --profile monitoring up -d
  # ─────────────────────────────────────────
  grafana:
    image: grafana/grafana:latest
    container_name: poc-grafana
    restart: unless-stopped
    networks: [poc-pecek-net]
    profiles: [monitoring]
    environment:
      - GF_SECURITY_ADMIN_PASSWORD=${GRAFANA_PASSWORD}
      - GF_SERVER_ROOT_URL=https://admin.${DOMAIN}/grafana
    volumes:
      - grafana-data:/var/lib/grafana
      - ./monitoring/grafana/dashboards:/var/lib/grafana/dashboards:ro
    ports:
      - "127.0.0.1:3001:3000"
    depends_on:
      - prometheus
